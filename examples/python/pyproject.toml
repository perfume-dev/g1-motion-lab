[project]
name = "g1-motion-lab-viewer"
version = "0.1.0"
description = "Local, kinematic-only G1 reference playback with OpenGL"
requires-python = ">=3.12,<3.15"
dependencies = [
  "glfw==2.10.2",
  "moderngl==5.12.0",
  "numpy==2.5.2",
  "pillow==12.3.0",
]

[tool.uv]
package = false
